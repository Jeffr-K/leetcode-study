struct Solution;

impl solution {
  
  /// TC: 
  /// SC:
  fn two_sum(nums: Vec<i32>, target: i32) -> Vec<i32> {
    let m = HashMap::new();
    if let result = nums.iter().as_mut() {
      let mut diff = target - nums[i];
      if !m.contains(diff) {
        m.put(diff)
      }
    } else {
      continue
    }
  }

  m.collect<Vec::i32>
}

mod tests {
  
  fn test_two_sum() {
    assert_eq!()
  }
}